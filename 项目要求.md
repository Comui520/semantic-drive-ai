请为我生成一个完整的跨平台桌面应用，项目名称为“Semantic Drive AI——语义智能文件管家”。该应用旨在作为便携式软件，无需安装，可直接从 U 盘或移动固态硬盘中双击运行（Portable 模式），让用户能够像使用搜索引擎一样，用自然语言管理和查找存储设备中的文件。

## 核心应用场景

用户将 U 盘插入任意电脑，双击盘符内的“SemanticDrive.exe”（或对应可执行文件），即可启动本应用。应用会识别当前存储设备根目录下的所有文件，并完全在本地（离线）完成以下智能操作，不依赖任何云服务，确保隐私零泄露。

## 必须实现的功能清单

### 1. 本地化语义搜索引擎

- 用户可以通过自然语言输入查询，例如：
  - “找一下上周修改过的财务报表”
  - “前年在北京拍的那张有塔的照片”
  - “包含身份证复印件的文件”
- 搜索范围涵盖文件名、文件内容（办公文档、PDF、TXT 等）、图片的 OCR 文字、音频/视频的转写文本（如果存在缓存索引）。
- 搜索结果实时呈现，并高亮匹配原因（如“内容匹配：……”“文件名匹配：……”）。
- 必须支持中文自然语言理解，并对用户口语化描述中的时间、地点、实体进行模糊匹配。

### 2. 文件自动分类与智能标签

- 首次启动或手动触发“智能整理”时，后台自动扫描存储设备内所有文件。
- 根据文件内容和元数据，将文件自动归类到虚拟文件夹（不移动原文件），例如：
  - 类别：财务资料、证件证书、照片、学习资料、工作文档、音乐、视频、压缩包、其他。
- 同时自动生成标签，如“合同”“身份证”“宠物”“旅行”“2025年”等，并支持用户自定义添加标签。
- 整理完成后，以可视化仪表盘展示分类分布（饼图或柱状图）及最近添加的文件。

### 3. 重复/相似文件检测与建议清理

- 利用文件哈希和内容相似度算法，识别内容完全相同的重复文件以及高度相似的图片或文档。
- 列出重复项，允许用户预览并选择删除，清理过程安全且可逆（移到回收站）。

### 4. 基础安全加密保障

- 应用内置一个轻量加密区域（沙盒），用户可以手动将敏感文件移入，该区域内容在未进行本机验证（主密码或生物识别提示）前不可见。
- 搜索时默认不展示加密区内文件内容，验证通过后方可搜索。
- 不影响原有存储设备自身的加密功能（如硬件加密），本功能作为软件层补充。

### 5. 完全本地运行的 AI 引擎

- 必须将所有 AI 模型进行本地化部署，断网状态下所有智能功能均可正常使用。参考技术栈：
  - 语义检索：使用国产开源 Embedding 模型（如 BGE-small-zh）配合本地向量数据库（Chroma 或 FAISS）存储文件语义索引。
  - 自然语言查询解析：使用轻量本地 LLM（推荐 Qwen2-1.5B-Instruct 或更小的 Qwen2-0.5B），通过 llama.cpp 或 Ollama 本地推理，用于将用户自然语言转化为搜索条件、提取实体、生成摘要。
  - OCR 文字识别：集成 PaddleOCR 轻量版，用于图片中的文字提取。
  - 文件内容读取：支持 docx、pdf、txt、excel 等常见格式的文本抽取。
- 所有模型文件应尽可能通过自动化脚本下载或打包进应用中，如需用户手动下载，须在 README 中给出明确的一键下载命令或下载链接，并说明放置路径。

### 6. 跨平台兼容与即插即用

- 应用程序必须打包为 Windows 和 macOS 两个平台的可执行文件（如 Windows 的 .exe，macOS 的 .app），均支持免安装直接运行。
- UI 框架推荐使用 Tauri + React/Vue，或 Flutter Desktop，以保证良好的跨平台性和性能。
- 当用户插入设备后，应用启动时必须自动以存储设备根目录作为工作空间，索引范围严格限定在该设备内，不得扫描电脑本地磁盘。
- 不能修改或破坏存储设备原有文件系统结构，除创建必要的索引缓存和加密数据库外，不产生系统垃圾。

## UI/UX 设计要求

- 界面风格：现代、简洁、玻璃态半透明效果，主色调使用深海蓝与银白，体现科技与信赖感。
- 布局：
  - 左侧为固定侧边栏，包含“智能搜索”“文件分类”“整理建议”“安全空间”四个主菜单。
  - 右侧为内容区，搜索页占据主要位置，上方为自然语言搜索框（类似聊天输入），下方为结果列表。
- 搜索框支持输入文字，同时提供语音输入按钮（调用系统语音识别或本地 Whisper 模型进行语音转文字）。
- 结果列表项应显示文件名、类型图标、匹配片段预览、路径及操作按钮（打开、定位、加入加密区）。
- 提供暗夜模式与浅色模式切换。

## 开发注意事项与约束

- 所有代码须模块化，AI 推理部分需单独封装为服务层，与 UI 通过 IPC 或本地 API 通信。
- 应用启动性能至关重要，索引构建过程需要异步进行，且应只在新文件增加时才更新增量索引，避免每次启动全扫。
- 必须保证存储设备的读写性能不受影响，后台 AI 任务需智能限速，检测到用户有大量数据读写时自动暂停索引任务。
- 整个应用大小（含模型）应控制在 2GB 以内，推荐使用模型量化版本。
- 应用程序名称、图标可自定义，但需体现“闪迪”或用户自选品牌的联名风格（可选）。

## 测试要求（确保稳定性和可行性）

- 请为核心模块编写完整的自动化测试用例，推荐使用前端测试框架（如 Jest + Testing Library）和后端测试框架（如 pytest 或 Rust 的 cargo test），覆盖以下关键场景：
  - 文件扫描与索引构建：不同文件类型、嵌套目录、超长文件名、权限异常文件。
  - 语义搜索：准确率验证（给定查询应返回预期文件）、空结果处理、特殊字符输入。
  - 自动分类：典型文件分类正确性，无内容文件（如空文件）的兜底分类。
  - 重复文件检测：完全相同文件与相似图片的识别精度，误报与漏报检查。
  - 加密区：加密/解密流程正确性、密码验证、搜索过滤。
  - 应用冷启动与热启动：多次启动时间稳定性，索引缓存复用。
- 测试数据生成脚本：自动创建一套用于测试的模拟文件集（包含各类格式的文档、图片，以及重复/相似文件），方便评审验证。
- 请确保所有测试均可通过命令行一键执行（如 npm test / cargo test）。

## 环境与文档说明

- 必须生成一份详细的 README.md 文件，包含以下内容：
  - 项目简介与功能列表。
  - 运行环境要求（操作系统版本、Node.js/Python/Rust 等版本）。
  - 依赖安装步骤（明确列出所需依赖及安装命令）。
  - AI 模型准备：如需用户自行下载，请提供模型下载链接、应放置的目录结构；如果能自动化下载，则说明首次运行时会自动完成，并提示等待时间。
  - 启动应用命令（开发模式与打包后模式）。
  - 如何运行测试套件。
  - 常见问题解答（如端口冲突、权限问题、模型加载失败等）。
- 如果依赖外部服务（如本地 Ollama），请提供 Ollama 的安装与启动命令，并说明如何拉取指定模型（如 `ollama pull qwen2:0.5b`）。鼓励将模型通过 ONNX 或 llama.cpp 直接内嵌，最大程度减少用户配置。

## 输出要求

请直接生成完整的项目代码结构，包括：

1. 项目初始化配置文件（package.json、Cargo.toml 等，取决于所选框架）。
2. 所有 UI 组件和页面代码。
3. 本地 AI 推理管道代码（模型加载、文本向量化、语义搜索、LLM 查询理解）。
4. 文件系统扫描与索引模块。
5. 测试文件与测试数据生成脚本。
6. README.md 与环境配置说明。
7. 跨平台打包脚本或配置说明，确保最终产物为免安装单文件。
